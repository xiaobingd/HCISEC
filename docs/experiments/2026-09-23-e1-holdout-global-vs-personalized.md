# E1-H：CycleGAN 留出池上的全局阈值与个性化阈值

- 实验 ID：`E1-HOLDOUT-20260923`
- 日期：2026-09-23
- 状态：已完成，但协议被确认过度限制；结果仅作数据饥饿诊断，不作为模型最终结论
- 目的：在修复跨阶段训练—测试泄漏后，重新评估九位注册者，并比较全局阈值与个性化阈值
- 结论级别：低成本、梯度隔离协议；不是预先设计的严格 outer-holdout 最终实验

## 2026-09-23 代码与协议复核更正

用户指出旧版认证头整体效果正常后，重新对照旧/新 checkpoint 与训练日志，确认：

- 新旧均使用 `trainb.py + modelb.py`；
- 均为 `MultiSourceAuthModel`、SupCon、100 epochs、batch size 32；
- 均启用 shallow、bottleneck、cycle-diff；
- checkpoint 均有179个 state keys，模型配置一致；
- 可训练参数均为825,216。

性能下降的主要原因不是更换了认证头代码，而是本轮错误地把九个人都限制为各自 CycleGAN validation+test，导致认证头训练量从旧版1524条降到326条，注册模板从旧协议约28条/人降到5–6条/人。

这一限制是不必要的。对于某个目标用户的单人 CycleGAN：

1. 只有该目标用户的数据参与过该 CycleGAN；
2. 其他八名用户的全部数据均未参与该目标 CycleGAN；
3. 更重要的是，认证头可以与 CycleGAN 共用训练集；只要最终认证测试严格保留在未见的 CycleGAN test 中，就不存在最终测试泄漏。

因此本页32.07% EER只能说明“用极少 holdout 数据训练认证头并仅用5–6条注册”效果差，不能据此否定原认证头结构。正确的低成本修复协议应是：

- 目标用户 CycleGAN train：用于认证头训练；
- 目标用户 CycleGAN validation：拆分 enrollment 与 threshold calibration；
- 目标用户 CycleGAN test：仅用于最终 genuine 测试；
- 其他用户：为该目标认证头单独固定 auth train/validation/test，确保该头的最终 impostor test 不进入认证头训练；
- 每套目标认证头保存独立 manifest，并强制检查最终 genuine/impostor test 与认证头训练交集为0。

本页结果保留用于完整溯源，但不再作为“模型整体失效”或“不能进入E2”的直接证据。

## 为什么执行

旧 E1 的认证测试样本大量进入过相应 CycleGAN 或旧认证头训练，不能作为独立泛化证据。本实验复用每位用户历史 CycleGAN 的 validation+test 作为认证候选池，避免重训九套 CycleGAN，同时强制保持与 CycleGAN 梯度训练集隔离。

## 固定数据边界

- 九用户原始总样本：2691；
- CycleGAN validation+test 认证候选池：544；
- 认证头训练：326；
- 认证验证池：109；
- 固定最终测试：109；
- CycleGAN train 与认证候选池交集：0；
- 认证 train/validation/test 两两交集：0；
- 九套认证头使用完全相同的 split manifest；
- manifest SHA-256：`d349b25d214166af31049a9e9763e11c6195414d1573851f081cb9bdb4b29924`。

验证池对每位用户按 50% enrollment / 50% threshold calibration 划分，使用 seeds=`42,123,456,789,2026`。最终测试集在所有 seed 下固定。每位注册者使用与其单人 CycleGAN 绑定的独立共享认证头。

注意：EER 会随 seed 变化，因为 seed 同时改变 enrollment 样本和注册模板，进而改变测试分数；若只改变阈值校准而模板和测试分数固定，EER 才不应变化。

## 宏平均结果（5 seeds）

|方法|FAR|FRR|HTER|Balanced Accuracy|Accuracy|Test EER|Worst-user HTER|
|---|---:|---:|---:|---:|---:|---:|---:|
|Personalized|0.3523±0.0128|0.2755±0.0355|0.3139±0.0161|0.6861±0.0161|0.6561±0.0103|0.3207±0.0126|0.5085±0.0664|
|Global|0.3681±0.0343|0.2718±0.0261|0.3200±0.0072|0.6800±0.0072|0.6428±0.0278|0.3207±0.0126|0.4785±0.0373|

个性化阈值将宏平均 HTER 从31.995%降到31.388%，仅改善约0.61个百分点；同时 worst-user HTER 反而高于全局阈值。因此当前数据不支持“个性化阈值显著解决困难用户问题”的强结论。

## 逐用户结果（5 seeds 均值±样本标准差）

|用户|Personal HTER|Global HTER|Personal BA|Global BA|Test EER|主要误接收来源（平均条数）|
|---|---:|---:|---:|---:|---:|---|
|bo|0.0551±0.0874|0.0103±0.0036|0.9449±0.0874|0.9897±0.0036|0.0010±0.0023|user4:2.0, user7:0.4|
|user2|0.5066±0.0702|0.4716±0.0435|0.4934±0.0702|0.5284±0.0435|0.5026±0.0596|user9:10.4, user6:9.6|
|user3|0.3535±0.0507|0.3536±0.0348|0.6465±0.0507|0.6464±0.0348|0.3762±0.0433|user6:10.4, user9:10.4|
|user4|0.3663±0.0357|0.3953±0.0248|0.6337±0.0357|0.6047±0.0248|0.3451±0.0331|user7:8.0, bo:4.8|
|user5|0.2359±0.0342|0.2681±0.0371|0.7641±0.0342|0.7319±0.0371|0.2383±0.0293|user8:8.0, user3:6.6|
|user6|0.3845±0.0431|0.4022±0.0138|0.6155±0.0431|0.5978±0.0138|0.3938±0.0291|user3:8.4, user9:7.6|
|user7|0.3433±0.0270|0.3626±0.0910|0.6567±0.0270|0.6374±0.0910|0.3979±0.0316|user4:6.6, user5:5.0|
|user8|0.3227±0.0661|0.3384±0.0632|0.6773±0.0661|0.6616±0.0632|0.3607±0.0473|user6:9.4, user3:8.8|
|user9|0.2570±0.0251|0.2774±0.0057|0.7430±0.0251|0.7226±0.0057|0.2704±0.0299|user3:10.6, user5:8.6|

## 解释

1. **bo 是明显特例。** bo 的平均测试 EER 约0.10%，但不能代表系统对其他用户普遍有效。
2. **user2 接近随机。** EER 约50.26%，Personalized BA 约49.34%，应暂时标记为不可认证用户，而不是混入总体有效性结论。
3. **user3、user4、user6、user7、user8 仍很困难。** EER 约34.5%–39.8%，明显不满足可靠身份认证要求。
4. **用户对混淆普遍存在。** 除 bo 外，多数注册者对若干特定冒认用户有大量误接受，支持 pair-specific confusion 的存在。
5. **旧5.54% EER不能保留为正式结果。** 隔离后宏平均 EER 约32.07%，差异过大，说明旧协议的数据泄漏对结论产生了实质影响。
6. **创新点1目前不能以“九用户普遍低 EER”成立。** 更可信的研究问题是：为什么单人模态转换仅对部分用户有效，以及如何通过用户质量筛选、困难用户拒绝策略、跨用户负样本和域不变特征改善泛化。

## 暂时剔除规则建议

不能根据本次 test EER 直接决定剔除，否则会对测试集调参。建议预先在 validation/calibration 上定义质量门槛，例如：

- calibration EER ≥ 40%，或
- enrollment/calibration 分离度低于固定阈值，或
- 五个 seed 中超过半数未优于随机基线。

然后冻结规则，仅在 test 上报告“全体用户”和“通过质量门槛的用户”两套结果。user2 可作为首个待审计对象，但正式剔除必须按上述验证集规则重算。

## 结果文件与完整性

远程仅保存聚合结果，不保存到 GitHub 的原始分数或特征：

- 评估脚本 SHA-256：`0ce50a191e2d8fc94f1f0133cf458ad9272df91dd8f0ab75db907272c16e2623`
- 聚合 JSON SHA-256：`b995234ebb5baa720aeeb7616e26c42703a47d00b0bde99fdfb4575cd5456885`
- 逐用户聚合 CSV SHA-256：`850bd70de75caa4a27dceaacda3686f82df85d885867074ceac5b95957f387e2`

GitHub 未保存原始样本、嵌入、逐样本分数、凭据或模型二进制文件。

## 远程清理记录

为释放临时数据盘，仅删除明确可再生或已被替代的内容：

- user2–user9 已完成 CycleGAN 的 epoch 50/100/150 中间检查点，共24个文件，约6.58 GiB；
- 保留每位用户的 epoch 200 checkpoint、final model、训练日志和可视化；
- 删除15个无 final model 的废弃目录：旧350-epoch尝试、OOM失败目录和从未启动的占位目录；
- 保留原始数据、当前九套 CycleGAN、当前九套认证头、split manifest、评估结果和历史对照记录；
- 临时数据盘占用由约90%降至77%，释放约6.5 GiB；
- 远程删除不可恢复，但保留的最终模型与日志足以继续本研究流程。

## 下一步

1. 在 validation/calibration 上实现预先定义的用户质量门槛，不查看 test 决策；
2. 分别报告全体九用户与通过质量门槛用户的结果；
3. 对 user2 进行标签、近邻相似度、采集会话和传感器质量审计；
4. 针对高频混淆用户对加入 hard-negative 或 pair-aware 训练；
5. 在上述问题解决前，不进入以性能提升为前提的 E2 正式实验。
