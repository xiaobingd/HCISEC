# E1-B：Global Threshold vs Personalized Threshold

- 实验 ID：`e1-b-global-vs-personalized-threshold-20260922`
- 日期：2026-09-22
- 状态：已完成（探索性；不作为独立测试泛化结果）
- 目的：比较统一全局阈值与逐用户个性化阈值
- 用户范围：bo、user2–user9
- 随机种子：42、0、1、2、3
- 测试集：固定使用 E1-A 的评估划分
- 模型参数：不更新；复用 E1-A 已生成的逐用户模型和特征
- 数据原则：只在 AutoDL 远程实例按用户隔离读取；GitHub 只保存聚合指标

## 阈值定义

对每个 seed：

- 个性化阈值：每个用户使用自己的校准 genuine/impostor 分数确定 `tau_u`；
- 全局阈值：汇总 9 个用户校准数据的 genuine/impostor 分数，确定一个共同的 `tau_g`；
- 两种阈值都只使用校准数据；
- 两种方法在完全相同的测试样本上比较 FAR、FRR、HTER 和 Balanced Accuracy。

## 结果

| 方法 | FAR | FRR | HTER | Balanced Accuracy | Accuracy |
|---|---:|---:|---:|---:|---:|
| Global Threshold | 5.292% | 5.605% | 5.449% | 94.551% | 94.673% |
| Personalized Threshold | 5.589% | 5.939% | 5.764% | 94.236% | 94.372% |

| 方法 | 最大 HTER | 最低 Balanced Accuracy |
|---|---:|---:|
| Global Threshold | 9.320% | 90.680% |
| Personalized Threshold | 9.069% | 90.931% |

## 有效性修正

E1-B 复用了在全量数据上训练得到的 CycleGAN 和认证头。虽然阈值计算没有使用测试标签，但测试样本已经参与过模型训练，因此本结果只能作为训练集内的阈值策略探索，不能作为严格泛化比较。

## 解释

当前探索性结果显示，个性化阈值没有改善宏平均 HTER，但对 worst-user HTER 有轻微改善。该结论需要在严格外层留出集重训后重新验证。

本实验的全局阈值还直接汇总了 9 个独立认证头的校准分数；正式实验应先做分数校准或标准化，再比较全局阈值。

## 数据隔离

- E1-B 评估脚本内部的集合交集为 0。
- 训练历史使用过全量数据，因此不能宣称模型训练与测试完全隔离。
- 原始用户数据、特征数组和模型文件未上传 GitHub。

## 后续

按预先固定的外层训练集/独立留出集协议重训所有受影响模型，再重复 E1-A 和 E1-B。