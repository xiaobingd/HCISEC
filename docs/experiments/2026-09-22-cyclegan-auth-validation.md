# CycleGAN 逐用户身份认证验证

- 实验 ID：`cyclegan-auth-validation-20260922`
- 日期：2026-09-22
- 状态：进行中
- 用户范围：bo、user2–user9
- 记录原则：只提交汇总指标与脱敏元数据，不提交原始用户数据或特征

## 目的

验证“每个注册用户拥有独立 CycleGAN 模态转换模型和独立认证头”时的身份认证效果，并对每个注册者分别统计认证性能。

## 模型绑定

每个用户的验证运行绑定：

1. 该用户自己的 CycleGAN 生成器检查点；
2. 该用户自己的认证头检查点；
3. 相同的验证协议和数据划分；
4. 一对多身份认证统计。

不同用户之间不复用生成器或认证头参数。验证过程只在远程实例内读取数据，仓库不保存样本、特征数组或模型二进制文件。

## 已记录的步骤

### 1. 建立认证头

- bo、user2–user9 各自完成认证头训练并生成最终模型。
- 九套认证头模型文件均已确认存在。
- 训练结束后没有遗留认证头训练进程。
- 模型文件只保存在远程文件存储盘。

### 2. 启动逐用户验证

验证脚本：远程 `audit_all_users.py`

每个用户使用自己的认证头和自己的 CycleGAN 检查点，输出目录按用户隔离到远程文件存储盘。仓库只接收完成后的汇总结果。

### 3. 启动问题与修正

- 首次启动未产生验证结果。
- 原因：后台子进程未继承输出根目录变量，触发未定义变量退出。
- 影响范围：未读取或上传原始数据；未形成有效实验结果。
- 修正：停止无效队列，改用固定绝对路径重新启动。
- 当前状态：修正后的验证队列已重新启动。

## 资源记录

- GPU：单卡 NVIDIA GeForce RTX 3080 Ti
- 远程文件存储盘：约 5% 使用
- 临时数据盘：约 94% 使用
- 约束：后续结果继续写入文件存储盘，避免临时盘继续增长。

## 待补充

验证完成后追加：

- 每个用户的 EER、FAR、FRR、平衡准确率、准确率
- 一对多认证的宏平均结果
- 认证阈值和固定数据划分说明
- 每个模型的 SHA-256（仅模型完整性校验，不上传模型）
- 失败重试与最终采用结果
- 结果解释、限制和下一步建议

## 数据隔离声明

本条目不包含原始用户数据、原始特征、样本内容、认证凭据或可逆恢复用户数据的文件。所有结果提交前必须先做脱敏检查。
