# CycleGAN 逐用户身份认证验证

- 实验 ID：`cyclegan-auth-validation-20260922`
- 日期：2026-09-22
- 状态：已完成
- 用户范围：bo、user2–user9
- 记录原则：只提交汇总指标与脱敏元数据，不提交原始用户数据或特征

## 目的

验证“每个注册用户拥有独立 CycleGAN 模态转换模型和独立认证头”时的身份认证效果，并对每个注册者分别统计认证性能。

## 模型绑定

每个用户的验证运行绑定：

1. 该用户自己的 CycleGAN 生成器检查点；
2. 该用户自己的认证头检查点；
3. 相同的验证协议和数据划分；
4. 一对多身份认证统计。

不同用户之间不复用生成器或认证头参数。验证过程只在远程实例内读取数据，仓库不保存样本、特征数组或模型二进制文件。

## 已记录的步骤

### 1. 建立认证头

- bo、user2–user9 各自完成认证头训练并生成最终模型。
- 九套认证头模型文件均已确认存在。
- 训练结束后没有遗留认证头训练进程。
- 模型文件只保存在远程文件存储盘。

### 2. 启动逐用户验证

验证脚本：远程 `audit_all_users.py`

每个用户使用自己的认证头和自己的 CycleGAN 检查点，输出目录按用户隔离到远程文件存储盘。仓库只接收完成后的汇总结果。

验证协议使用固定 seed=42 的划分；训练/验证/测试样本交集检查均为 0。

### 3. 启动问题与修正

- 首次启动未产生验证结果。
- 原因：后台子进程未继承输出根目录变量，触发未定义变量退出。
- 影响范围：未读取或上传原始用户数据；未形成有效实验结果。
- 修正：停止无效队列，改用固定绝对路径重新启动。
- user2 首次验证还因其 CycleGAN 检查点目录名称不同而失败。
- 原因：user2 使用的是 `samebo200_user2`，不是其他用户使用的 `samebo200safe_user2`。
- 修正：确认专属检查点存在后，仅重跑 user2；最终结果采用重跑结果。

## 最终结果

以下是每个验证运行中“该运行对应注册者”的一对多认证指标。结果来自固定 seed=42 的 `fixed_threshold` 评估：

| 注册者 | EER | FAR | FRR | 平衡准确率 | 准确率 |
|---|---:|---:|---:|---:|---:|
| bo | 0.029193 | 0.035491 | 0.000000 | 0.982255 | 0.966601 |
| user2 | 0.166852 | 0.189310 | 0.166667 | 0.822012 | 0.813360 |
| user3 | 0.166852 | 0.189310 | 0.166667 | 0.822012 | 0.813360 |
| user4 | 0.101959 | 0.146667 | 0.084746 | 0.884294 | 0.860511 |
| user5 | 0.098998 | 0.035635 | 0.150000 | 0.907183 | 0.950884 |
| user6 | 0.134595 | 0.204900 | 0.133333 | 0.830883 | 0.803536 |
| user7 | 0.121808 | 0.084633 | 0.166667 | 0.874350 | 0.905697 |
| user8 | 0.233593 | 0.146993 | 0.233333 | 0.809837 | 0.842829 |
| user9 | 0.066741 | 0.120267 | 0.050000 | 0.914866 | 0.888016 |

### 结果解释

- bo 的认证效果最好，EER 最低，平衡准确率最高。
- user4、user5、user9 的效果较好。
- user2、user3、user6、user7 居中。
- user8 的 EER 最高、FRR 较高，是当前最需要进一步分析的用户。
- 指标反映的是固定划分下的实验结果，不等同于跨时间、跨设备或开放环境下的最终部署性能。

## user2/user3 重复性核查

用户提出 user2 和 user3 可能数据重复，已在远程实例内完成哈希核查：

- 原始文件 SHA-256 不同（仅保留远程核验结果，不提交原始数据）。
- user2 数据形状为 300×80×81，user3 为 297×80×81。
- 逐样本精确哈希交集为 0。
- 两套 CycleGAN 模型 SHA-256 不同。
- 两套认证头 SHA-256 不同。
- 两套验证特征文件 SHA-256 不同，逐元素完全相同比例为 0。
- 对应认证分数向量也不同（最大绝对差异约 1.56，平均绝对差异约 0.66）。

结论：当前证据不支持 user2/user3 原始数据重复，也不支持结果文件复制。两者本轮指标相同，是在相同固定划分下最终分类计数恰好一致；仍建议后续用多个随机种子和独立时间切分复核稳定性。

## 资源记录

- GPU：单卡 NVIDIA GeForce RTX 3080 Ti
- 远程文件存储盘：约 5% 使用
- 临时数据盘：约 94% 使用
- 约束：后续结果继续写入文件存储盘，避免临时盘继续增长。

## 数据隔离声明

本条目不包含原始用户数据、原始特征、样本内容、认证凭据或可逆恢复用户数据的文件。所有结果提交前均经过脱敏，仅保留汇总指标和过程说明。
